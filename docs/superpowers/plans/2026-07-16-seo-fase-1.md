# SEO Fase 1 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fazer os 6 roteiros à venda existirem no Google — renderizados no servidor a partir do banco, com URL limpa e sitemap automático — e recuperar com `301` as ~20 URLs do WordPress que hoje são 404.

**Architecture:** O banco (`po_roteiros`) vira fonte única. `roteiro.php` e `roteiros.php` buscam o Supabase **no servidor** (REST + anon key, cache em disco) e devolvem HTML completo; `sitemap.php` gera o sitemap do banco. O `.htaccess` faz o rewrite das URLs limpas e o mapa de `301`. Todo o markup que hoje vive em `assets/js/roteiro-dynamic.js` é **portado para PHP e o JS de render é deletado** — não pode haver template em duas linguagens.

**Tech Stack:** PHP 8 (cPanel/ereHost, sem Composer), Apache `.htaccess`, Supabase REST, HTML/CSS/JS estático. Testes: PHP puro com `assert()` + suíte `curl`.

Spec: `docs/superpowers/specs/2026-07-16-seo-indexacao-roteiros-design.md`

## Global Constraints

- **PHP sem dependências externas** — cPanel sem Composer. Só stdlib + `curl`. Seguir o padrão do `enviar.php`: defaults no topo do arquivo, sobrescritos por `config.local.php` se existir.
- **NUNCA sobrescrever `config.local.php` no deploy** — segredos vivem só no servidor. Está no `.gitignore`.
- **Filtrar `ativo=eq.true` EXPLICITAMENTE em toda query.** Verificado em 16/07/2026: o banco tem **0 linhas inativas**, então o teste de RLS é inconclusivo — não se sabe se a política existe. Não confiar nela. Roteiro rascunho vazando = roteiro rascunho indexado.
- **Copy Freela:** português, sem travessões, sem emojis, números concretos.
- **WhatsApp:** sempre `https://wa.me/5548996048882` (com o `55`).
- **Slug = identidade estável da viagem**, sem data/tema/parada da edição.
- **Nunca escrever dado de roteiro à mão em HTML** (regra do CLAUDE.md — dado duplicado envelhece e pisca).
- **Cache-buster obrigatório** ao mexer em CSS/JS: o `.htaccess` cacheia por 1 mês.
- Deploy é **manual por FTPS** (ver CLAUDE.md). Senha via `.netrc` no scratchpad, nunca inline.

---

### Task 1: PHP local e a suíte de verificação

**Files:**
- Create: `tests/run.php`
- Create: `tools/seo-check.sh`
- Modify: `.gitignore`

**Interfaces:**
- Produces: `php tests/run.php` roda todos os `tests/test-*.php`. `bash tools/seo-check.sh <base-url>` roda as asserções de HTTP.

- [ ] **Step 1: Instalar PHP**

```bash
winget install --id PHP.PHP.8.3 -e --accept-source-agreements --accept-package-agreements
```

Abrir um shell novo e conferir:

```bash
php -v
```
Expected: `PHP 8.3.x`. Se `php` não for encontrado, achar o caminho (`ls "/c/Program Files/PHP"`) e adicionar ao PATH da sessão.

- [ ] **Step 2: Conferir que as extensões necessárias existem**

```bash
php -m | grep -iE "^(curl|json|mbstring)$"
```
Expected: as três linhas `curl`, `json`, `mbstring`. Sem `curl` nada funciona.

- [ ] **Step 3: Criar o runner de testes**

`tests/run.php`:
```php
<?php
/* Runner mínimo: roda todo tests/test-*.php num processo separado.
   Sem PHPUnit de propósito — a hospedagem não tem Composer e o projeto
   não tem build. */
$dir  = __DIR__;
$fail = 0;
foreach (glob($dir . '/test-*.php') as $f) {
    $out  = [];
    $code = 0;
    exec(escapeshellarg(PHP_BINARY) . ' ' . escapeshellarg($f) . ' 2>&1', $out, $code);
    $name = basename($f);
    if ($code === 0) {
        echo "PASS  $name\n";
    } else {
        $fail++;
        echo "FAIL  $name\n      " . implode("\n      ", $out) . "\n";
    }
}
echo $fail ? "\n$fail arquivo(s) de teste falharam\n" : "\nTodos os testes passaram\n";
exit($fail ? 1 : 0);
```

- [ ] **Step 4: Criar a suíte de HTTP**

`tools/seo-check.sh`:
```bash
#!/usr/bin/env bash
# Asserções de SEO contra uma base URL.
#   bash tools/seo-check.sh https://pereiraoliveiraturismo.com.br
# Sem argumento usa http://localhost:8080 (php -S).
BASE="${1:-http://localhost:8080}"
PASS=0; FAIL=0

chk() { # chk <descricao> <esperado> <obtido>
  if [ "$2" = "$3" ]; then PASS=$((PASS+1)); printf '  ok   %s\n' "$1"
  else FAIL=$((FAIL+1)); printf '  FAIL %s\n       esperado: %s\n       obtido:   %s\n' "$1" "$2" "$3"; fi
}
code() { curl -s -o /dev/null -w '%{http_code}' --max-redirs 0 "$1"; }
loc()  { curl -s -o /dev/null -w '%{redirect_url}' --max-redirs 0 "$1"; }
body() { curl -s "$1"; }

echo "== $BASE"
chk "/roteiros responde 200"            200 "$(code "$BASE/roteiros")"
chk "/roteiros/turquia responde 200"    200 "$(code "$BASE/roteiros/turquia")"
chk "slug inexistente da 404"           404 "$(code "$BASE/roteiros/nao-existe-xyz")"
chk "sitemap responde 200"              200 "$(code "$BASE/sitemap.xml")"

# o conteudo tem que existir SEM javascript
b=$(body "$BASE/roteiros/turquia")
chk "roteiro traz o titulo no HTML"     ok "$(echo "$b" | grep -qi 'turquia'            && echo ok || echo no)"
chk "roteiro traz JSON-LD TouristTrip"  ok "$(echo "$b" | grep -q  'TouristTrip'        && echo ok || echo no)"
chk "roteiro traz o dia a dia"          ok "$(echo "$b" | grep -q  'class=\"day__p\"'   && echo ok || echo no)"
chk "roteiro NAO tem noindex"           ok "$(echo "$b" | grep -qi 'noindex'            && echo no || echo ok)"
chk "roteiro tem canonical"             ok "$(echo "$b" | grep -q  'rel=\"canonical\"'  && echo ok || echo no)"

s=$(body "$BASE/sitemap.xml")
chk "sitemap lista a turquia"           ok "$(echo "$s" | grep -q '/roteiros/turquia'   && echo ok || echo no)"
chk "sitemap NAO lista roteiro extinto" ok "$(echo "$s" | grep -q 'mercados-de-natal'   && echo no || echo ok)"

echo; echo "  $PASS ok, $FAIL falhas"; [ "$FAIL" -eq 0 ]
```

- [ ] **Step 5: Ignorar o cache local**

Acrescentar ao `.gitignore`:
```
# cache do render server-side (gerado em runtime)
cache/
```

- [ ] **Step 6: Rodar o runner vazio para provar que ele funciona**

```bash
php tests/run.php
```
Expected: `Todos os testes passaram` (nenhum teste ainda — exit 0).

- [ ] **Step 7: Commit**

```bash
git add tests/run.php tools/seo-check.sh .gitignore
git commit -m "test: runner de testes PHP e suite de asseroes de SEO"
```

---

### Task 2: `lib/po-data.php` — dados do Supabase com cache

**Files:**
- Create: `lib/po-data.php`
- Test: `tests/test-po-data.php`

**Interfaces:**
- Produces:
  - `po_config(): array` → `['SUPABASE_URL' => string, 'SUPABASE_ANON_KEY' => string]`
  - `po_set_fetcher(?callable $f): void` — injeta HTTP fake nos testes. `$f(string $url): ?string` devolve o body ou `null`.
  - `po_fetch_roteiros(): array` — lista de roteiros **ativos**, ordenada. `[]` se indisponível.
  - `po_fetch_roteiro(string $slug): ?array` — um roteiro **ativo**, ou `null`.
  - `po_ano(array $r): string` — ano da viagem (`'2026'`) a partir de `data_label`/`periodo`; `''` se não achar.
  - `po_cache_dir(): string`

- [ ] **Step 1: Escrever o teste que falha**

`tests/test-po-data.php`:
```php
<?php
require __DIR__ . '/../lib/po-data.php';

function ok($cond, $msg) { if (!$cond) { fwrite(STDERR, "ASSERT: $msg\n"); exit(1); } }

// --- po_ano: extrai o ano dos formatos reais do banco
ok(po_ano(['data_label' => 'De 27/10/26 à 12/11/26']) === '2026', 'po_ano dd/mm/yy com prefixo');
ok(po_ano(['data_label' => '22/02/27 à 15/03/27'])    === '2027', 'po_ano dd/mm/yy sem prefixo');
ok(po_ano(['data_label' => '', 'periodo' => '10/12/2025 a 21/12/2025']) === '2025', 'po_ano cai no periodo, ano com 4 digitos');
ok(po_ano(['data_label' => 'Datas a definir']) === '', 'po_ano sem data devolve vazio');

// --- fetcher injetado: nao toca a rede
$fake = json_encode([[ 'slug' => 'turquia', 'titulo' => 'Turquia', 'ativo' => true ]]);
$capturado = '';
po_set_fetcher(function ($url) use ($fake, &$capturado) { $capturado = $url; return $fake; });

$r = po_fetch_roteiro('turquia');
ok($r !== null && $r['slug'] === 'turquia', 'po_fetch_roteiro devolve o registro');

// O ponto critico: o banco tem 0 linhas inativas, entao o teste de RLS e
// inconclusivo. O filtro TEM que estar na query, nao na confianca.
ok(strpos($capturado, 'ativo=eq.true') !== false, 'po_fetch_roteiro filtra ativo=eq.true na URL');
ok(strpos($capturado, 'slug=eq.turquia') !== false, 'po_fetch_roteiro filtra por slug');

po_set_fetcher(function ($url) use (&$capturado) { $capturado = $url; return '[]'; });
ok(po_fetch_roteiro('nao-existe') === null, 'slug inexistente devolve null');
ok(po_fetch_roteiros() === [], 'lista vazia devolve array vazio');
ok(strpos($capturado, 'ativo=eq.true') !== false, 'po_fetch_roteiros filtra ativo=eq.true');

// --- rede fora do ar nao pode explodir
po_set_fetcher(function ($url) { return null; });
ok(po_fetch_roteiro('turquia') === null, 'fetcher nulo devolve null, nao excecao');
ok(po_fetch_roteiros() === [], 'fetcher nulo devolve [], nao excecao');

// --- json invalido tambem nao
po_set_fetcher(function ($url) { return 'isto nao e json'; });
ok(po_fetch_roteiro('turquia') === null, 'json invalido devolve null');

po_set_fetcher(null);
echo "po-data ok\n";
```

- [ ] **Step 2: Rodar e ver falhar**

```bash
php tests/run.php
```
Expected: `FAIL test-po-data.php` com `Failed opening required '../lib/po-data.php'`.

- [ ] **Step 3: Implementar**

`lib/po-data.php`:
```php
<?php
/* ============================================================
   Pereira Oliveira — camada de dados dos roteiros (server-side).

   POR QUE NO SERVIDOR: a pagina de roteiro precisa existir em HTML
   para o Google. Ate 16/07/2026 ela era montada no navegador e
   marcada noindex, ou seja, os roteiros a venda nao existiam na
   busca.

   ANON KEY: e publica por natureza (roda no navegador em
   assets/js/po-config.js). Nao e segredo. O que NAO pode aparecer
   aqui e a service_role.
============================================================ */

$SUPABASE_URL      = 'https://euzmbswywwhmicjlszqw.supabase.co';
$SUPABASE_ANON_KEY = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImV1em1ic3d5d3dobWljamxzenF3Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODA0NDEyODYsImV4cCI6MjA5NjAxNzI4Nn0.oSIv6fSKVxO9Umuii6xt98cT0yoSqepTIzVCdcocfuU';

if (is_file(__DIR__ . '/../config.local.php')) {
    require __DIR__ . '/../config.local.php';
}

function po_config() {
    global $SUPABASE_URL, $SUPABASE_ANON_KEY;
    return ['SUPABASE_URL' => $SUPABASE_URL, 'SUPABASE_ANON_KEY' => $SUPABASE_ANON_KEY];
}

$GLOBALS['PO_FETCHER'] = null;
function po_set_fetcher($f) { $GLOBALS['PO_FETCHER'] = $f; }

function po_cache_dir() {
    // sys_get_temp_dir e gravavel no cPanel e NAO e servido pela web.
    $d = sys_get_temp_dir() . '/po-cache';
    if (!is_dir($d)) { @mkdir($d, 0700, true); }
    return $d;
}

/* GET no Supabase, com cache em disco. TTL curto: o painel edita e a
   cliente quer ver a mudanca no site sem esperar. */
function po_http_get($url, $ttl = 600) {
    if ($GLOBALS['PO_FETCHER']) {
        $f = $GLOBALS['PO_FETCHER'];
        return $f($url);
    }
    $file = po_cache_dir() . '/' . sha1($url) . '.json';
    if (is_file($file) && (time() - filemtime($file)) < $ttl) {
        $c = @file_get_contents($file);
        if ($c !== false) return $c;
    }
    if (!function_exists('curl_init')) return null;
    $cfg = po_config();
    $ch  = curl_init($url);
    curl_setopt_array($ch, [
        CURLOPT_RETURNTRANSFER => true,
        CURLOPT_TIMEOUT        => 8,
        CURLOPT_HTTPHEADER     => [
            'apikey: ' . $cfg['SUPABASE_ANON_KEY'],
            'Authorization: Bearer ' . $cfg['SUPABASE_ANON_KEY'],
            'Accept: application/json',
        ],
    ]);
    $body = curl_exec($ch);
    $code = curl_getinfo($ch, CURLINFO_HTTP_CODE);
    curl_close($ch);

    if ($body === false || $code < 200 || $code >= 300) {
        error_log('PO roteiros fetch falhou (' . $code . ')');
        // Supabase fora do ar: serve cache velho em vez de pagina vazia.
        // Pagina vazia indexada e pior que pagina desatualizada.
        if (is_file($file)) { $c = @file_get_contents($file); if ($c !== false) return $c; }
        return null;
    }
    @file_put_contents($file, $body, LOCK_EX);
    return $body;
}

function po_rest($query, $ttl = 600) {
    $cfg = po_config();
    $body = po_http_get(rtrim($cfg['SUPABASE_URL'], '/') . '/rest/v1/po_roteiros?' . $query, $ttl);
    if ($body === null) return null;
    $d = json_decode($body, true);
    return is_array($d) ? $d : null;
}

/* ativo=eq.true e OBRIGATORIO e explicito.
   Em 16/07/2026 o banco tinha 0 linhas inativas, entao "anon so ve ativos"
   passou por vacuidade: nao ha prova de que a RLS filtre. Se ela nao filtrar
   e a gente confiar, o primeiro rascunho da cliente vai pro ar e pro Google. */
function po_fetch_roteiros() {
    $d = po_rest('select=*&ativo=eq.true&order=ordem.asc,created_at.asc');
    return $d === null ? [] : $d;
}

function po_fetch_roteiro($slug) {
    $d = po_rest('select=*&ativo=eq.true&slug=eq.' . rawurlencode($slug) . '&limit=1');
    return (is_array($d) && count($d)) ? $d[0] : null;
}

/* Ano da viagem, para o <title> ("Excursao Turquia 2026 ...").
   Formatos reais no banco: "De 27/10/26 a 12/11/26", "25/11/26 a 02/12/26",
   "10/12/2025 a 21/12/2025". Pega a PRIMEIRA data. */
function po_ano($r) {
    $s = trim((string) (($r['data_label'] ?? '') !== '' ? $r['data_label'] : ($r['periodo'] ?? '')));
    if ($s === '') return '';
    if (preg_match('~\b\d{1,2}/\d{1,2}/(\d{4})\b~', $s, $m)) return $m[1];
    if (preg_match('~\b\d{1,2}/\d{1,2}/(\d{2})\b~', $s, $m)) return '20' . $m[1];
    if (preg_match('~\b(20\d{2})\b~', $s, $m)) return $m[1];
    return '';
}
```

- [ ] **Step 4: Rodar e ver passar**

```bash
php tests/run.php
```
Expected: `PASS test-po-data.php`.

- [ ] **Step 5: Provar contra o banco de verdade**

```bash
php -r 'require "lib/po-data.php"; $l = po_fetch_roteiros(); echo count($l) . " roteiros\n"; foreach ($l as $r) { echo "  " . $r["slug"] . " | ano=" . po_ano($r) . "\n"; }'
```
Expected: 6 roteiros, cada um com ano preenchido (2026/2027). Se algum ano vier vazio, o `po_ano` não cobre aquele formato — corrigir e voltar ao Step 1.

- [ ] **Step 6: Commit**

```bash
git add lib/po-data.php tests/test-po-data.php
git commit -m "feat: camada de dados server-side dos roteiros

Filtra ativo=eq.true explicitamente: o banco tem 0 linhas inativas,
entao nao ha prova de que a RLS filtre rascunho."
```

---

### Task 3: `lib/po-view.php` — chrome compartilhado

**Files:**
- Create: `lib/po-view.php`
- Test: `tests/test-po-view.php`

**Interfaces:**
- Consumes: `po_ano()` da Task 2.
- Produces:
  - `po_e(?string $s): string` — escape HTML (equivalente ao `esc()` do JS).
  - `po_url(string $path): string` — URL absoluta canônica (`https://pereiraoliveiraturismo.com.br/...`).
  - `po_roteiro_href(array $r): string` → `/roteiros/<slug>`
  - `po_head(array $o): string` — `<head>` completo. Chaves: `title`, `description`, `canonical`, `og_image`, `css` (array), `jsonld` (array|null), `noindex` (bool).
  - `po_header(): string`, `po_footer(): string`, `po_wa_float(): string`
  - `po_titulo_seo(array $r): string` — `"Excursão Turquia 2026 · Viagem em grupo saindo de Florianópolis"`

- [ ] **Step 1: Escrever o teste que falha**

`tests/test-po-view.php`:
```php
<?php
require __DIR__ . '/../lib/po-data.php';
require __DIR__ . '/../lib/po-view.php';

function ok($cond, $msg) { if (!$cond) { fwrite(STDERR, "ASSERT: $msg\n"); exit(1); } }

ok(po_e('<script>&"') === '&lt;script&gt;&amp;&quot;', 'po_e escapa < > & aspas');
ok(po_e(null) === '', 'po_e aceita null');
ok(po_url('/roteiros') === 'https://pereiraoliveiraturismo.com.br/roteiros', 'po_url absolutiza');
ok(po_roteiro_href(['slug' => 'turquia']) === '/roteiros/turquia', 'href do roteiro');

$r = ['titulo' => 'Turquia com Antália', 'data_label' => 'De 27/10/26 à 12/11/26'];
$t = po_titulo_seo($r);
ok(strpos($t, 'Excursão') === 0, 'titulo comeca com Excursao (a palavra que o publico busca)');
ok(strpos($t, '2026') !== false, 'titulo traz o ano');
ok(strpos($t, 'Florianópolis') !== false, 'titulo traz a origem, que e o diferencial sem concorrencia');

$semdata = po_titulo_seo(['titulo' => 'Turquia', 'data_label' => '']);
ok(strpos($semdata, '  ') === false, 'sem ano nao deixa espaco duplo');

$h = po_head([
    'title' => 'X', 'description' => 'D', 'canonical' => po_url('/roteiros'),
    'og_image' => po_url('/assets/images/roteiro-grecia.jpg'), 'css' => ['assets/css/roteiro.css?v=7'],
    'jsonld' => ['@context' => 'https://schema.org', '@type' => 'TouristTrip'],
]);
ok(strpos($h, '<title>X</title>') !== false, 'head tem title');
ok(strpos($h, 'rel="canonical"') !== false, 'head tem canonical');
ok(strpos($h, 'og:image') !== false, 'head tem og:image');
ok(strpos($h, 'TouristTrip') !== false, 'head embute o json-ld');
ok(strpos($h, 'GT-TNH4L3BV') !== false, 'head mantem a Google Tag');
ok(strpos($h, 'noindex') === false, 'head sem noindex por padrao');

$hn = po_head(['title' => 'X', 'description' => '', 'canonical' => '', 'og_image' => '', 'css' => [], 'noindex' => true]);
ok(strpos($hn, 'noindex') !== false, 'noindex quando pedido');

ok(strpos(po_header(), 'wa.me/5548996048882') !== false, 'header tem o whatsapp com o 55');
ok(strpos(po_footer(), 'wa.me/5548996048882') !== false, 'footer tem o whatsapp com o 55');
echo "po-view ok\n";
```

- [ ] **Step 2: Rodar e ver falhar**

```bash
php tests/run.php
```
Expected: `FAIL test-po-view.php` — `lib/po-view.php` não existe.

- [ ] **Step 3: Implementar**

`lib/po-view.php`: portar o chrome a partir do `roteiro.html` (header linhas 20-45, e o footer/WhatsApp do mesmo arquivo). O `<head>` segue o padrão das páginas estáticas (`grecia-terra-mar.html` linhas 1-40) — Google Tag, preconnect das fontes, favicon, theme-color, apple-touch-icon, OG e Twitter Card.

```php
<?php
/* Chrome compartilhado das paginas PHP. O markup e porte 1:1 do
   roteiro.html (header) e das paginas estaticas (head/footer), para o
   CSS existente continuar valendo sem uma linha nova. */

define('PO_BASE', 'https://pereiraoliveiraturismo.com.br');

function po_e($s) { return htmlspecialchars((string) ($s ?? ''), ENT_QUOTES, 'UTF-8'); }
function po_url($path) { return PO_BASE . '/' . ltrim((string) $path, '/'); }
function po_roteiro_href($r) { return '/roteiros/' . rawurlencode($r['slug']); }

/* O <title> mira como o publico de 60+ escreve: "excursao" (nao "roteiro",
   que e jargao de blogueiro), o ano (data fixa e o produto) e a saida de
   Florianopolis (o diferencial que ela disputa com quase ninguem). */
function po_titulo_seo($r) {
    $ano   = po_ano($r);
    $part  = 'Excursão ' . trim((string) $r['titulo']);
    if ($ano !== '') $part .= ' ' . $ano;
    return $part . ' · Viagem em grupo saindo de Florianópolis';
}

function po_head($o) {
    $noindex = !empty($o['noindex']);
    $css = '';
    foreach (($o['css'] ?? []) as $c) $css .= '  <link rel="stylesheet" href="' . po_e($c) . '">' . "\n";
    $jsonld = '';
    if (!empty($o['jsonld'])) {
        $jsonld = '  <script type="application/ld+json">'
            . json_encode($o['jsonld'], JSON_UNESCAPED_SLASHES | JSON_UNESCAPED_UNICODE)
            . '</script>' . "\n";
    }
    $robots = $noindex ? 'noindex, nofollow' : 'index, follow, max-image-preview:large';
    $h  = '<!DOCTYPE html>' . "\n" . '<html lang="pt-BR">' . "\n" . '<head>' . "\n";
    $h .= '  <meta charset="UTF-8">' . "\n";
    $h .= '  <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">' . "\n";
    $h .= '  <script async src="https://www.googletagmanager.com/gtag/js?id=GT-TNH4L3BV"></script>' . "\n";
    $h .= '  <script>window.dataLayer=window.dataLayer||[];function gtag(){dataLayer.push(arguments);}gtag(\'js\',new Date());gtag(\'config\',\'GT-TNH4L3BV\');</script>' . "\n";
    $h .= '  <meta name="robots" content="' . $robots . '">' . "\n";
    $h .= '  <title>' . po_e($o['title']) . '</title>' . "\n";
    $h .= '  <meta name="description" content="' . po_e($o['description'] ?? '') . '">' . "\n";
    if (!empty($o['canonical'])) $h .= '  <link rel="canonical" href="' . po_e($o['canonical']) . '">' . "\n";
    $h .= '  <meta property="og:type" content="website">' . "\n";
    $h .= '  <meta property="og:site_name" content="Pereira Oliveira Turismo">' . "\n";
    $h .= '  <meta property="og:locale" content="pt_BR">' . "\n";
    $h .= '  <meta property="og:title" content="' . po_e($o['title']) . '">' . "\n";
    $h .= '  <meta property="og:description" content="' . po_e($o['description'] ?? '') . '">' . "\n";
    if (!empty($o['canonical'])) $h .= '  <meta property="og:url" content="' . po_e($o['canonical']) . '">' . "\n";
    if (!empty($o['og_image'])) {
        $h .= '  <meta property="og:image" content="' . po_e($o['og_image']) . '">' . "\n";
        $h .= '  <meta property="og:image:alt" content="' . po_e($o['title']) . '">' . "\n";
    }
    $h .= '  <meta name="twitter:card" content="summary_large_image">' . "\n";
    $h .= '  <meta name="theme-color" content="#1FA8DD">' . "\n";
    $h .= '  <link rel="icon" href="/assets/images/favicon.png">' . "\n";
    $h .= '  <link rel="apple-touch-icon" href="/assets/images/favicon.png">' . "\n";
    $h .= '  <link rel="preconnect" href="https://fonts.googleapis.com">' . "\n";
    $h .= '  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>' . "\n";
    $h .= '  <link href="https://fonts.googleapis.com/css2?family=Rubik:wght@400;500;600;700&family=DM+Sans:wght@400;500;600&family=DM+Mono:wght@400;500&display=swap" rel="stylesheet">' . "\n";
    $h .= $css . $jsonld;
    $h .= '</head>' . "\n" . '<body>' . "\n";
    return $h;
}
```

`po_header()`, `po_footer()` e `po_wa_float()`: copiar o markup **literalmente** de `roteiro.html` (header: linhas 20-45; footer e botão flutuante: ver o fim de `grecia-terra-mar.html`), trocando os `href` relativos (`index.html`, `nossa-historia.html`, `contato.html`) por absolutos de raiz (`/`, `/nossa-historia.html`, `/contato.html`) — **obrigatório**, porque `/roteiros/turquia` tem um nível a mais de path e link relativo quebraria. No `po_header()`, trocar `index.html#inicio` por `/roteiros`.

- [ ] **Step 4: Rodar e ver passar**

```bash
php tests/run.php
```
Expected: `PASS test-po-view.php` e `PASS test-po-data.php`.

- [ ] **Step 5: Commit**

```bash
git add lib/po-view.php tests/test-po-view.php
git commit -m "feat: chrome compartilhado das paginas PHP"
```

---

### Task 4: `roteiro.php` — a página do roteiro

**Files:**
- Create: `roteiro.php`
- Test: `tests/test-roteiro-page.php`

**Interfaces:**
- Consumes: tudo de `lib/po-data.php` e `lib/po-view.php`.
- Produces: `po_roteiro_html(array $r): string` (corpo) e `po_roteiro_jsonld(array $r): array`. O arquivo, chamado via web com `?slug=`, imprime a página inteira.

- [ ] **Step 1: Escrever o teste que falha**

`tests/test-roteiro-page.php`:
```php
<?php
define('PO_TEST', 1); // impede o arquivo de despachar sozinho
require __DIR__ . '/../lib/po-data.php';
require __DIR__ . '/../lib/po-view.php';
require __DIR__ . '/../roteiro.php';

function ok($cond, $msg) { if (!$cond) { fwrite(STDERR, "ASSERT: $msg\n"); exit(1); } }

$r = [
    'slug' => 'turquia', 'titulo' => 'Turquia com Antália', 'subtitulo' => 'Istambul e Capadócia',
    'descricao_curta' => 'Uma viagem pela Turquia.', 'data_label' => 'De 27/10/26 à 12/11/26',
    'dias' => 17, 'local_label' => 'Turquia', 'capa_url' => 'https://x/capa.jpg',
    'roteiro_dias' => [['n' => 1, 'data' => '27/10', 'dia_semana' => 'terça', 'cidades' => 'São Paulo', 'descricao' => 'Embarque <b>internacional</b>.', 'refeicoes' => 'Jantar']],
    'inclui' => ['Passagem aérea'], 'nao_inclui' => ['Bebidas'], 'hoteis' => [['cidade' => 'Istambul', 'hotel' => 'Hotel X']],
    'valores' => [['tag' => 'Duplo', 'valor' => 'R$ 40.000', 'extra' => 'em 10x']],
    'galeria' => ['https://x/1.jpg', 'https://x/2.jpg'],
];

$h = po_roteiro_html($r);
ok(strpos($h, 'Turquia com Antália') !== false, 'corpo tem o titulo');
ok(strpos($h, 'class="day__p"') !== false, 'corpo tem o dia a dia em texto');
ok(strpos($h, 'Embarque &lt;b&gt;internacional&lt;/b&gt;') !== false, 'descricao do dia vem escapada (dado vem do painel)');
ok(strpos($h, 'wa.me/5548996048882') !== false, 'corpo tem o whatsapp');
ok(strpos($h, 'R$ 40.000') !== false, 'corpo tem o investimento');
ok(strpos($h, 'invest__cards--single') !== false, 'um valor so usa o modificador single');
ok(substr_count($h, 'gal__item') === 2, 'galeria com 2 itens');
ok(strpos($h, 'id="f-roteiro"') !== false, 'formulario de lead presente');
ok(strpos($h, 'value="site-roteiro-turquia"') !== false, 'origem do lead carrega o slug');

$j = po_roteiro_jsonld($r);
ok($j['@type'] === 'TouristTrip', 'json-ld e TouristTrip');
ok($j['name'] === 'Turquia com Antália', 'json-ld tem o nome');
ok(isset($j['offers']), 'json-ld tem offers');

// sem valores nao inventa oferta
$j2 = po_roteiro_jsonld(array_merge($r, ['valores' => []]));
ok(!isset($j2['offers']), 'sem valores nao emite offers falsa');

// campos faltando nao podem explodir
$min = ['slug' => 'x', 'titulo' => 'X'];
$hm = po_roteiro_html($min);
ok(is_string($hm) && strlen($hm) > 0, 'roteiro minimo nao explode');
echo "roteiro-page ok\n";
```

- [ ] **Step 2: Rodar e ver falhar**

```bash
php tests/run.php
```
Expected: `FAIL test-roteiro-page.php` — `roteiro.php` não existe.

- [ ] **Step 3: Implementar — porte do JS para PHP**

`roteiro.php` é o **porte 1:1** de `assets/js/roteiro-dynamic.js`. A tradução é mecânica:

| JS | PHP |
|---|---|
| `esc(x)` | `po_e($x)` |
| `arr(x)` | `po_arr($x)` (helper novo, abaixo) |
| `'a' + b + 'c'` | interpolação em heredoc |
| `.map(f).join('')` | `foreach` acumulando string |
| `window.poIntroMedia(r)` | `po_intro_media($r)` — porte de `roteiros-shared.js:61-85` |
| `window.poHeroVideo(r)` | `po_hero_video($r)` — porte de `roteiros-shared.js:99-107` |

Cabeçalho e helpers:
```php
<?php
require_once __DIR__ . '/lib/po-data.php';
require_once __DIR__ . '/lib/po-view.php';

function po_arr($a) { return is_array($a) ? $a : []; }

const PO_WHY = [
    'Cada aspecto da viagem é meticulosamente planejado por uma equipe dedicada, com profundo conhecimento dos destinos.',
    'Nossa experiência em viagens de grupo cria uma dinâmica harmoniosa e uma convivência agradável entre os viajantes.',
    'Um coordenador da Pereira Oliveira acompanha o grupo desde a saída do Brasil, com suporte contínuo em todo o roteiro.',
    'Trabalhamos com uma rede de hotéis e fornecedores criteriosamente selecionados, para garantir conforto e bom atendimento.',
];
const PO_CHECK = '<span class="why__ic"><svg viewBox="0 0 24 24"><path d="M20 6L9 17l-5-5" stroke-linecap="round" stroke-linejoin="round"/></svg></span>';
```

`po_hero_video()` difere do JS num ponto: **não dá para checar `prefers-reduced-motion` no servidor**. O JS fazia `matchMedia(...)` e omitia o `<video>`. Em PHP, sempre emitir o `<video class="hero__vid">` e deixar o CSS resolver:

```css
/* assets/css/roteiro.css — acrescentar */
@media (prefers-reduced-motion: reduce) { .hero__vid { display: none; } }
```
Isso muda o comportamento: quem pede menos animação **baixa o vídeo e não o vê**, em vez de não baixar. É o preço de renderizar no servidor. Aceitável: o vídeo capa tem ~1,3 MB e o ganho de SEO é o objetivo da fase. **Registrar no CLAUDE.md.**

`po_roteiro_jsonld($r)`:
```php
function po_roteiro_jsonld($r) {
    $j = [
        '@context'    => 'https://schema.org',
        '@type'       => 'TouristTrip',
        'name'        => (string) ($r['titulo'] ?? ''),
        'description' => (string) ($r['descricao_curta'] ?? ''),
        'url'         => po_url(po_roteiro_href($r)),
        'provider'    => [
            '@type' => 'TravelAgency',
            'name'  => 'Pereira Oliveira Turismo',
            'url'   => PO_BASE,
        ],
    ];
    if (!empty($r['capa_url'])) $j['image'] = $r['capa_url'];
    $v = po_arr($r['valores'] ?? []);
    // So emite offers com preco de verdade. Preco inventado no schema e
    // divergente da pagina e violacao de diretriz do Google.
    if (count($v) && !empty($v[0]['valor'])) {
        $raw   = (string) $v[0]['valor'];
        $moeda = (strpos($raw, '€') !== false) ? 'EUR' : ((strpos($raw, 'US$') !== false) ? 'USD' : 'BRL');
        $num   = preg_replace('~[^\d,\.]~', '', $raw);
        $num   = str_replace(['.', ','], ['', '.'], $num);
        if ($num !== '' && is_numeric($num)) {
            $j['offers'] = [
                '@type'         => 'Offer',
                'price'         => $num,
                'priceCurrency' => $moeda,
                'availability'  => 'https://schema.org/InStock',
                'url'           => po_url(po_roteiro_href($r)),
            ];
        }
    }
    return $j;
}
```

`po_roteiro_html($r)`: portar as seções de `roteiro-dynamic.js` nesta ordem, mantendo **exatamente** as mesmas classes CSS (o `roteiro.css` já existe e não deve mudar):
- HERO — `roteiro-dynamic.js:47-59`. Trocar os `href` do breadcrumb: `index.html` → `/`, `index.html#inicio` → `/roteiros`, e o texto "Roteiros" continua.
- INTRO/por que viajar — `:61-67`
- DIA A DIA — `:69-83`
- INFORMAÇÕES — `:85-94`
- INVESTIMENTO — `:96-112`
- GALERIA — `:114-121`
- OUTROS ROTEIROS — `:123-129`, **mas server-side**: em vez do `<div id="more-track">` vazio preenchido por JS, emitir os `<a href="/roteiros/<slug>">` de cada outro roteiro **no HTML**. Link interno é sinal de SEO e não pode depender de JS. Buscar com `po_fetch_roteiros()` e pular o `slug` atual.
- MODAL do lead — `roteiro-dynamic.js:134-155`, idêntico.

Despacho no fim do arquivo:
```php
if (!defined('PO_TEST')) {
    $slug = isset($_GET['slug']) ? (string) $_GET['slug'] : '';
    $r    = $slug === '' ? null : po_fetch_roteiro($slug);
    if (!$r) {
        // 404 DE VERDADE. Servir 200 com "nao encontrado" cria soft-404:
        // o Google indexa a pagina de erro.
        http_response_code(404);
        header('Content-Type: text/html; charset=utf-8');
        readfile(__DIR__ . '/404.html');
        exit;
    }
    $desc = trim((string) ($r['descricao_curta'] ?? ''));
    if ($desc === '') $desc = 'Viagem em grupo para ' . $r['titulo'] . ' com saída de Florianópolis e acompanhamento da Pereira Oliveira Turismo, desde 1967.';
    header('Content-Type: text/html; charset=utf-8');
    echo po_head([
        'title'       => po_titulo_seo($r),
        'description' => mb_substr($desc, 0, 155),
        'canonical'   => po_url(po_roteiro_href($r)),
        'og_image'    => $r['capa_url'] ?? po_url('/assets/images/roteiro-grecia.jpg'),
        'css'         => ['/assets/css/roteiro.css?v=7'],
        'jsonld'      => po_roteiro_jsonld($r),
    ]);
    echo po_header();
    echo '<main id="rt-main">' . po_roteiro_html($r) . '</main>';
    echo po_roteiro_modal($r);
    echo po_footer();
    echo po_wa_float();
    echo '<script src="/assets/js/roteiro.js?v=3"></script>' . "\n";
    echo '<script src="/assets/js/lead-form.js?v=2"></script>' . "\n";
    echo '</body></html>';
}
```

- [ ] **Step 4: Rodar e ver passar**

```bash
php tests/run.php
```
Expected: `PASS test-roteiro-page.php`.

- [ ] **Step 5: Ver no navegador, com dado real**

```bash
php -S localhost:8080 &
curl -s "http://localhost:8080/roteiro.php?slug=turquia-com-antalia" | head -40
```
Expected: HTML com `<title>Excursão Turquia com Antália 2026 · ...`, `TouristTrip`, e o dia a dia. **Sem JavaScript nenhum executado.**

- [ ] **Step 6: Commit**

```bash
git add roteiro.php tests/test-roteiro-page.php assets/css/roteiro.css
git commit -m "feat: pagina de roteiro renderizada no servidor

Porte 1:1 do roteiro-dynamic.js. Slug inexistente devolve 404 real
em vez de 200 com 'nao encontrado', que viraria soft-404 indexado."
```

---

### Task 5: `roteiros.php` — a página mãe

**Files:**
- Create: `roteiros.php`
- Create: `assets/css/roteiros.css`
- Test: `tests/test-roteiros-page.php`

**Interfaces:**
- Produces: `po_roteiros_html(array $lista): string`, `po_roteiros_jsonld(array $lista): array`

**Contexto:** é a peça de posicionamento. A home é tela travada e não serve de listagem para o Google; sem esta página, cada roteiro fica órfão. Ela também é o alvo de "viagem em grupo terceira idade" e "excursão saindo de Florianópolis".

- [ ] **Step 1: Escrever o teste que falha**

`tests/test-roteiros-page.php`:
```php
<?php
define('PO_TEST', 1);
require __DIR__ . '/../lib/po-data.php';
require __DIR__ . '/../lib/po-view.php';
require __DIR__ . '/../roteiros.php';

function ok($cond, $msg) { if (!$cond) { fwrite(STDERR, "ASSERT: $msg\n"); exit(1); } }

$lista = [
    ['slug' => 'turquia', 'titulo' => 'Turquia com Antália', 'data_label' => 'De 27/10/26 à 12/11/26', 'dias' => 17, 'capa_url' => 'https://x/a.jpg', 'descricao_curta' => 'A.', 'local_label' => 'Turquia'],
    ['slug' => 'chile-e-deserto-do-atacama', 'titulo' => 'Chile e Deserto do Atacama', 'data_label' => '25/11/26 a 02/12/26', 'dias' => 8, 'capa_url' => 'https://x/b.jpg', 'descricao_curta' => 'B.', 'local_label' => 'Chile'],
];
$h = po_roteiros_html($lista);

ok(substr_count($h, '<h1') === 1, 'exatamente um h1');
ok(strpos($h, 'href="/roteiros/turquia"') !== false, 'link do roteiro no HTML, nao no JS');
ok(strpos($h, 'href="/roteiros/chile-e-deserto-do-atacama"') !== false, 'link do segundo roteiro');
ok(strpos($h, 'Turquia com Antália') !== false, 'titulo do roteiro em texto');
ok(strpos($h, 'De 27/10/26') !== false, 'data em texto, e nao so na imagem');
ok(strpos($h, 'wa.me/5548996048882') !== false, 'whatsapp presente');

$vazio = po_roteiros_html([]);
ok(strpos($vazio, '<h1') !== false, 'sem roteiro ainda renderiza a pagina');
ok(strpos($vazio, 'WhatsApp') !== false || strpos($vazio, 'wa.me') !== false, 'sem roteiro ainda oferece contato');

$j = po_roteiros_jsonld($lista);
ok($j['@type'] === 'ItemList', 'json-ld e ItemList');
ok(count($j['itemListElement']) === 2, 'ItemList com os 2 roteiros');
echo "roteiros-page ok\n";
```

- [ ] **Step 2: Rodar e ver falhar**

```bash
php tests/run.php
```
Expected: `FAIL test-roteiros-page.php`.

- [ ] **Step 3: Implementar**

`roteiros.php`. O `<h1>` e o texto de abertura carregam a estratégia da §5 da spec:

```php
<?php
require_once __DIR__ . '/lib/po-data.php';
require_once __DIR__ . '/lib/po-view.php';

function po_roteiros_jsonld($lista) {
    $itens = [];
    foreach ($lista as $i => $r) {
        $itens[] = [
            '@type'    => 'ListItem',
            'position' => $i + 1,
            'name'     => (string) ($r['titulo'] ?? ''),
            'url'      => po_url(po_roteiro_href($r)),
        ];
    }
    return ['@context' => 'https://schema.org', '@type' => 'ItemList', 'itemListElement' => $itens];
}

function po_roteiros_html($lista) {
    $h  = '<section class="rts-hero"><div class="wrap">';
    $h .= '<p class="tag sec__eyebrow"><span></span>Próximas viagens</p>';
    $h .= '<h1 class="rts-hero__h">Viagens em grupo com saída de Florianópolis</h1>';
    $h .= '<p class="rts-hero__lead">A Pereira Oliveira organiza viagens em grupo desde 1967. '
        . 'Um coordenador brasileiro acompanha o grupo desde a saída do Brasil, os hotéis e os '
        . 'traslados já estão contratados, e as datas são fixas. Você viaja sem montar roteiro, '
        . 'sem dirigir e sem se preocupar com idioma.</p>';
    $h .= '</div></section>';

    $h .= '<section class="rts"><div class="wrap">';
    if (!count($lista)) {
        $h .= '<p class="rts__empty">Estamos montando as próximas saídas. '
            . 'Fale com a gente pelo WhatsApp para saber das novidades.</p>';
    } else {
        $h .= '<div class="rts__grid">';
        foreach ($lista as $r) {
            $href  = po_e(po_roteiro_href($r));
            $badge = $r['badge'] ?? (!empty($r['dias']) ? $r['dias'] . ' dias' : '');
            $data  = $r['data_label'] ?? ($r['periodo'] ?? '');
            $h .= '<article class="rts__card reveal">'
                . '<a class="rts__link" href="' . $href . '">'
                . '<div class="rts__img"><img src="' . po_e($r['capa_url'] ?? '') . '" alt="' . po_e($r['titulo'] ?? '') . '" loading="lazy"></div>'
                . '<div class="rts__body">'
                . ($badge ? '<span class="rts__badge">' . po_e($badge) . '</span>' : '')
                . '<h2 class="rts__h">' . po_e($r['titulo'] ?? '') . '</h2>'
                . ($data ? '<p class="rts__date">' . po_e($data) . '</p>' : '')
                . ($r['descricao_curta'] ?? '' ? '<p class="rts__desc">' . po_e($r['descricao_curta']) . '</p>' : '')
                . '<span class="rts__cta">Ver roteiro</span>'
                . '</div></a></article>';
        }
        $h .= '</div>';
    }
    $h .= '</div></section>';
    return $h;
}

if (!defined('PO_TEST')) {
    $lista = po_fetch_roteiros();
    header('Content-Type: text/html; charset=utf-8');
    echo po_head([
        'title'       => 'Viagens em grupo saindo de Florianópolis · Pereira Oliveira Turismo',
        'description' => 'Excursões em grupo com coordenador brasileiro, datas fixas e saída de Florianópolis. A Pereira Oliveira organiza viagens desde 1967.',
        'canonical'   => po_url('/roteiros'),
        'og_image'    => po_url('/assets/images/roteiro-grecia.jpg'),
        'css'         => ['/assets/css/roteiro.css?v=7', '/assets/css/roteiros.css?v=1'],
        'jsonld'      => po_roteiros_jsonld($lista),
    ]);
    echo po_header();
    echo '<main>' . po_roteiros_html($lista) . '</main>';
    echo po_footer();
    echo po_wa_float();
    echo '</body></html>';
}
```

`assets/css/roteiros.css`: grade de cards reaproveitando os tokens do `roteiro.css` (`--ink`, `--azul`, `--verde`). Grid `repeat(auto-fill, minmax(320px, 1fr))`, gap 28px, card com imagem 16:10 `object-fit:cover`, radius 18px, sombra suave, hover subindo 4px. Respeitar `prefers-reduced-motion`. Sem framework.

- [ ] **Step 4: Rodar e ver passar**

```bash
php tests/run.php
```
Expected: `PASS test-roteiros-page.php`.

- [ ] **Step 5: Ver com dado real**

```bash
curl -s "http://localhost:8080/roteiros.php" | grep -c 'rts__card'
```
Expected: `6`.

- [ ] **Step 6: Commit**

```bash
git add roteiros.php assets/css/roteiros.css tests/test-roteiros-page.php
git commit -m "feat: pagina mae /roteiros listando o banco em HTML

A home e tela travada e nao serve de listagem pro Google; sem esta
pagina cada roteiro fica orfao."
```

---

### Task 6: `sitemap.php` — sitemap gerado do banco

**Files:**
- Create: `sitemap.php`
- Delete: `sitemap.xml`
- Test: `tests/test-sitemap.php`

**Interfaces:**
- Produces: `po_sitemap_xml(array $lista): string`

**Contexto:** é o que entrega o requisito "novos roteiros indexam automaticamente".

- [ ] **Step 1: Escrever o teste que falha**

`tests/test-sitemap.php`:
```php
<?php
define('PO_TEST', 1);
require __DIR__ . '/../lib/po-data.php';
require __DIR__ . '/../lib/po-view.php';
require __DIR__ . '/../sitemap.php';

function ok($cond, $msg) { if (!$cond) { fwrite(STDERR, "ASSERT: $msg\n"); exit(1); } }

$x = po_sitemap_xml([
    ['slug' => 'turquia', 'updated_at' => '2026-07-16T10:00:00+00:00'],
    ['slug' => 'chile-e-deserto-do-atacama', 'updated_at' => null],
]);

ok(strpos($x, '<?xml') === 0, 'comeca com a declaracao xml');
ok(strpos($x, 'https://pereiraoliveiraturismo.com.br/roteiros/turquia') !== false, 'lista o roteiro');
ok(strpos($x, 'https://pereiraoliveiraturismo.com.br/roteiros</loc>') !== false, 'lista a pagina mae');
ok(strpos($x, 'https://pereiraoliveiraturismo.com.br/</loc>') !== false, 'lista a home');
ok(strpos($x, '/nossa-historia.html') !== false, 'lista a historia');
ok(strpos($x, '/contato.html') !== false, 'lista o contato');
ok(strpos($x, '<lastmod>2026-07-16</lastmod>') !== false, 'lastmod vem do updated_at');

// as 5 estaticas morreram: nao podem reaparecer
foreach (['mercados-de-natal', 'grecia-terra-mar', 'floracao-das-cerejeiras', 'encantos-do-mediterraneo'] as $morto) {
    ok(strpos($x, $morto) === false, "sitemap nao lista o roteiro extinto $morto");
}
ok(strpos($x, 'roteiro.html') === false, 'sitemap nao lista o template antigo');
ok(strpos($x, '?slug=') === false, 'sitemap nao usa URL com query');
echo "sitemap ok\n";
```

- [ ] **Step 2: Rodar e ver falhar**

```bash
php tests/run.php
```
Expected: `FAIL test-sitemap.php`.

- [ ] **Step 3: Implementar**

`sitemap.php`:
```php
<?php
require_once __DIR__ . '/lib/po-data.php';
require_once __DIR__ . '/lib/po-view.php';

function po_sitemap_xml($lista) {
    $hoje = gmdate('Y-m-d');
    $u = function ($loc, $mod, $pri) {
        return '  <url><loc>' . po_e($loc) . '</loc><lastmod>' . po_e($mod) . '</lastmod>'
             . '<priority>' . $pri . '</priority></url>' . "\n";
    };
    $x  = '<?xml version="1.0" encoding="UTF-8"?>' . "\n";
    $x .= '<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">' . "\n";
    $x .= $u(PO_BASE . '/', $hoje, '1.0');
    $x .= $u(PO_BASE . '/roteiros', $hoje, '0.9');
    $x .= $u(PO_BASE . '/nossa-historia.html', $hoje, '0.7');
    $x .= $u(PO_BASE . '/contato.html', $hoje, '0.7');
    foreach ($lista as $r) {
        $mod = !empty($r['updated_at']) ? substr((string) $r['updated_at'], 0, 10) : $hoje;
        $x .= $u(PO_BASE . '/roteiros/' . rawurlencode($r['slug']), $mod, '0.8');
    }
    $x .= '</urlset>' . "\n";
    return $x;
}

if (!defined('PO_TEST')) {
    header('Content-Type: application/xml; charset=utf-8');
    echo po_sitemap_xml(po_fetch_roteiros());
}
```

- [ ] **Step 4: Rodar e ver passar**

```bash
php tests/run.php
```
Expected: `PASS test-sitemap.php`.

- [ ] **Step 5: Apagar o sitemap estático**

```bash
git rm sitemap.xml
```
O `.htaccess` (Task 10) faz `/sitemap.xml` servir o `sitemap.php`. **Manter a URL `/sitemap.xml`** — é a que está submetida no Search Console e no `robots.txt`.

- [ ] **Step 6: Commit**

```bash
git add sitemap.php tests/test-sitemap.php
git commit -m "feat: sitemap gerado do banco

Roteiro novo no painel entra no sitemap sozinho. O sitemap estatico
anunciava 5 roteiros que nao existem mais no banco."
```

---

### Task 7: Front-end — apagar o render em JS e as 5 páginas estáticas

**Files:**
- Modify: `assets/js/roteiros-shared.js:10-20`
- Delete: `assets/js/roteiro-dynamic.js`, `roteiro.html`
- Delete: `grecia-terra-mar.html`, `mercados-de-natal.html`, `tesouros-asiaticos.html`, `floracao-das-cerejeiras.html`, `encantos-do-mediterraneo.html`
- Modify: `assets/js/roteiro.js`, `assets/js/historia.js` (catálogos)
- Modify: `index.html`, `nossa-historia.html`, `contato.html` (menu ganha "Roteiros")

**Interfaces:**
- Consumes: `/roteiros/<slug>` da Task 4.
- Produces: `window.poRoteiroHref(slug)` → `'/roteiros/' + slug`. `window.PO_STATIC_ROTEIRO` deixa de existir.

- [ ] **Step 1: Apontar o href para a URL nova**

Em `assets/js/roteiros-shared.js`, substituir as linhas 10-20 por:
```js
  // Todo roteiro vive no banco e e servido por /roteiros/<slug> (roteiro.php).
  // O mapa PO_STATIC_ROTEIRO morreu com as 5 paginas estaticas: ele mandava um
  // roteiro do banco para a pagina estatica de mesmo slug, que e a origem do
  // slug 'tesouros-asiaticos2'.
  window.poRoteiroHref = function (slug) {
    return '/roteiros/' + encodeURIComponent(slug);
  };
```

- [ ] **Step 2: Conferir que ninguém mais usa o mapa**

```bash
grep -rn "PO_STATIC_ROTEIRO\|STATIC_ROTEIRO" --include=*.js --include=*.html . | grep -v node_modules
```
Expected: só `painel/app.js:359-360` (tratado na Task 8). Qualquer outro ponto tem que ser corrigido agora.

- [ ] **Step 3: Apagar o render em JS e as páginas mortas**

```bash
git rm assets/js/roteiro-dynamic.js roteiro.html \
       grecia-terra-mar.html mercados-de-natal.html tesouros-asiaticos.html \
       floracao-das-cerejeiras.html encantos-do-mediterraneo.html
```

- [ ] **Step 4: Limpar os catálogos escritos à mão**

```bash
grep -n "grecia-terra-mar\|mercados-de-natal\|floracao-das-cerejeiras\|encantos-do-mediterraneo\|tesouros-asiaticos" assets/js/roteiro.js assets/js/historia.js
```
Remover todo catálogo de roteiro escrito à mão nesses dois arquivos e passar a ler de `poFetchRoteiros()`. Se um catálogo servia de fallback, **apagar sem substituir** — é a mesma decisão já tomada para o `FALLBACK` do `main.js` (dado duplicado envelhece; ver CLAUDE.md).

- [ ] **Step 5: Pôr "Roteiros" no menu**

Em `index.html`, `nossa-historia.html` e `contato.html`, no `.hd__nav`, o link de roteiros passa a apontar para a página mãe:
```html
<a href="/roteiros">Roteiros</a>
```
(hoje aponta para `index.html#inicio`, que é a home travada e não lista nada para o Google)

- [ ] **Step 6: Provar que não sobrou referência às páginas mortas**

```bash
grep -rn "grecia-terra-mar\.html\|mercados-de-natal\.html\|tesouros-asiaticos\.html\|floracao-das-cerejeiras\.html\|encantos-do-mediterraneo\.html\|roteiro\.html" \
  --include=*.html --include=*.js --include=*.php --include=*.xml . | grep -v node_modules | grep -v docs/
```
Expected: **nenhuma linha**. Se aparecer, corrigir antes de commitar — link para página apagada é 404 servido ao visitante.

- [ ] **Step 7: Bump do cache-buster**

Em toda referência a `roteiros-shared.js`, subir a versão (`?v=3`). Sem isso o visitante recorrente fica com o arquivo velho por até 1 mês e continua indo para `roteiro.html?slug=`.

- [ ] **Step 8: Commit**

```bash
git add -A
git commit -m "refactor: roteiro so existe no servidor; aposenta as 5 estaticas

Template em duas linguagens e fonte dupla de verdade, que e o bug que
ja mordeu a home. O PHP renderiza, o JS so interage."
```

---

### Task 8: Painel — truncagem de slug e link de visualização

**Files:**
- Modify: `painel/app.js:48` (slugify), `painel/app.js:359-360` (STATIC_ROTEIRO/pubHref)
- Test: `tests/test-slugify.mjs`

**Interfaces:**
- Produces: `slugify(s)` truncando em limite de palavra; `pubHref(slug)` → `../roteiros/<slug>`.

- [ ] **Step 1: Escrever o teste que falha**

`tests/test-slugify.mjs` (Node, porque o painel é JS de navegador):
```js
import assert from 'node:assert/strict';
import { readFileSync } from 'node:fs';

// extrai a funcao do app.js sem carregar o painel inteiro
const src = readFileSync(new URL('../painel/app.js', import.meta.url), 'utf8');
const m = src.match(/function slugify\([\s\S]*?\n\}/);
assert.ok(m, 'slugify encontrada no app.js');
const slugify = new Function(m[0] + '; return slugify;')();

// o bug real, reproduzido: 60 chars cortando no meio de "acompanhante"
const titulo = 'Um roteiro exclusivo pelo melhor da Escandinávia com acompanhante';
const s = slugify(titulo);
assert.ok(!s.endsWith('-'), 'nao termina com hifen');
assert.ok(s.length <= 60, 'respeita o limite de 60');
assert.ok(!s.includes('acompan') || s.includes('acompanhante'),
  'nao corta no meio da palavra: era isso que gerava ...-com-acompan');

assert.equal(slugify('Tesouros Asiáticos'), 'tesouros-asiaticos', 'acentos e espacos');
assert.equal(slugify('Chile: Santiago & Atacama'), 'chile-santiago-atacama', 'pontuacao');
assert.equal(slugify(''), '', 'vazio');
assert.equal(slugify(null), '', 'null');
console.log('slugify ok');
```

- [ ] **Step 2: Rodar e ver falhar**

```bash
node tests/test-slugify.mjs
```
Expected: falha em `nao corta no meio da palavra` — o slug atual termina em `-com-acompan`.

- [ ] **Step 3: Corrigir o slugify**

Substituir `painel/app.js:48` por:
```js
/* Trunca em limite de PALAVRA. O .slice(0,60) cru cortava no meio e gerou
   'um-roteiro-exclusivo-pelo-melhor-da-escandinavia-com-acompan' no banco.
   O slug e URL publica permanente: cortar palavra ao meio fica pra sempre. */
function slugify(s){
  const base=(s||'').toString().toLowerCase().normalize('NFD')
    .replace(/[̀-ͯ]/g,'').replace(/[^a-z0-9]+/g,'-').replace(/(^-|-$)/g,'');
  if(base.length<=60)return base;
  const corte=base.slice(0,60);
  const ult=corte.lastIndexOf('-');
  return (ult>0?corte.slice(0,ult):corte).replace(/-$/,'');
}
```

- [ ] **Step 4: Rodar e ver passar**

```bash
node tests/test-slugify.mjs
```
Expected: `slugify ok`.

- [ ] **Step 5: Matar o STATIC_ROTEIRO do painel**

Substituir `painel/app.js:359-360` por:
```js
function pubHref(slug){return '../roteiros/'+encodeURIComponent(slug);}
```

- [ ] **Step 6: Orientar a cliente no campo do slug**

Em `painel/app.js:401`, trocar o label. O slug virou URL pública permanente e a cliente precisa saber:
```js
<div class="field"><label>Slug (URL) — vira o endereço da página, ex: /roteiros/chile-atacama</label><input id="r-slug" value="${esc(r.slug||'')}" placeholder="gerado do título se vazio"><small class="hint">Use o destino, sem data nem tema da edição. Repetir o slug de uma edição anterior é o certo: a página herda o histórico no Google.</small></div>
```

- [ ] **Step 7: Commit**

```bash
git add painel/app.js tests/test-slugify.mjs
git commit -m "fix: slug trunca em limite de palavra e aponta pra /roteiros

O .slice(0,60) cru gerou ...-escandinavia-com-acompan no banco."
```

---

### Task 9: Banco — índice único parcial e renomeação dos slugs

**Files:**
- Create: `supabase/migrations/2026-07-16-slug-estavel.sql`

**Interfaces:**
- Consumes: nada. Produces: slugs finais que o `.htaccess` da Task 10 referencia.

**ATENÇÃO — ordem importa.** Renomear slug não quebra o site atual (o carrossel da home monta os links a partir do próprio banco, então ele se corrige sozinho). Mas o `.htaccess` da Task 10 aponta para os nomes NOVOS. Rodar esta task **antes** do deploy.

- [ ] **Step 1: Escrever a migration**

`supabase/migrations/2026-07-16-slug-estavel.sql`:
```sql
-- SEO fase 1 — slug como identidade estavel da viagem.
--
-- Regra: o slug e o destino, sem data/tema/parada da edicao. Uma edicao
-- ativa por slug de cada vez (confirmado com a cliente). Assim a edicao
-- nova HERDA a URL e a autoridade da anterior, em vez de a agencia
-- recomecar do zero no Google a cada ano.

-- 1) Renomeacoes. Nada disso esta indexado (a pagina dinamica era noindex),
--    entao renomear agora e de graca; depois custaria um 301.
update po_roteiros set slug = 'turquia'
  where slug = 'turquia-com-antalia';               -- Antalia e parada desta edicao

update po_roteiros set slug = 'chile-e-deserto-do-atacama'
  where slug = 'chile-santiago-e-deserto-do-atacama'; -- Santiago e escala

-- O '2' era contorno do mapa STATIC_ROTEIRO, que mandava um roteiro do banco
-- com slug 'tesouros-asiaticos' para a pagina estatica de mesmo nome. O mapa
-- morreu nesta fase, entao o nome esta livre.
update po_roteiros set slug = 'tesouros-asiaticos'
  where slug = 'tesouros-asiaticos2';

-- Truncado no meio da palavra pelo .slice(0,60) do painel (corrigido na Task 8).
update po_roteiros set slug = 'escandinavia'
  where slug = 'um-roteiro-exclusivo-pelo-melhor-da-escandinavia-com-acompan';

-- 2) Um ativo por slug. Parcial de proposito: edicoes arquivadas mantem o
--    slug na linha e nao conflitam, e roteiro.php serve so ativo=true.
create unique index if not exists po_roteiros_slug_ativo_uniq
  on po_roteiros (slug) where ativo;
```

- [ ] **Step 2: Conferir que não há colisão ANTES de criar o índice**

```bash
K='<ANON_KEY do assets/js/po-config.js>'
curl -s "https://euzmbswywwhmicjlszqw.supabase.co/rest/v1/po_roteiros?select=slug,ativo&ativo=eq.true" \
  -H "apikey: $K" -H "Authorization: Bearer $K" \
  | python -c "import sys,json;d=json.load(sys.stdin);s=[r['slug'] for r in d];print('slugs ativos:',len(s),'| unicos:',len(set(s)));print('COLISAO!' if len(s)!=len(set(s)) else 'sem colisao: o indice vai criar')"
```
Expected: `sem colisao: o indice vai criar`. Se houver colisão, o `create unique index` falha — resolver o duplicado antes.

- [ ] **Step 3: Rodar a migration**

Supabase → SQL Editor → colar o arquivo → Run.

- [ ] **Step 4: Verificar os slugs novos no banco**

```bash
K='<ANON_KEY>'
curl -s "https://euzmbswywwhmicjlszqw.supabase.co/rest/v1/po_roteiros?select=slug&ativo=eq.true&order=ordem" \
  -H "apikey: $K" -H "Authorization: Bearer $K"
```
Expected exatamente: `turquia`, `chile-e-deserto-do-atacama`, `tesouros-asiaticos`, `escandinavia`, `caminhos-da-india`, `coreia-do-sul-japao-dubai`. Nenhum com `2`, nenhum truncado.

- [ ] **Step 5: Provar que o índice pega**

Tentar ativar dois roteiros com o mesmo slug deve falhar. No SQL Editor:
```sql
-- deve dar erro de unique violation (e so um teste: nao commitar dado)
begin;
insert into po_roteiros (slug, titulo, ativo) values ('turquia', 'Teste colisao', true);
rollback;
```
Expected: `duplicate key value violates unique constraint "po_roteiros_slug_ativo_uniq"`. Se inserir sem erro, o índice não foi criado — voltar ao Step 3.

- [ ] **Step 6: Commit**

```bash
git add supabase/migrations/2026-07-16-slug-estavel.sql
git commit -m "feat(db): slug estavel por destino e um ativo por slug

Edicao nova herda a URL e a autoridade da anterior."
```

---

### Task 10: `.htaccess`, deploy e verificação em produção

**Files:**
- Modify: `.htaccess`
- Modify: `robots.txt`
- Modify: `CLAUDE.md`

**Interfaces:**
- Consumes: todas as tasks anteriores.

**Esta é a task de risco.** `.htaccess` quebrado derruba o site inteiro com 500. Ordem: backup, sobe, testa em segundos, restaura se falhar.

- [ ] **Step 1: Escrever as regras**

Acrescentar ao `.htaccess`, **logo após `RewriteEngine On`** e **antes** da regra de HTTPS:

```apache
# ------------------------------------------------------------
# Canonicalizacao: www -> nao-www (o www respondia 200 e o site
# existia em duas versoes para o Google)
# ------------------------------------------------------------
RewriteCond %{HTTP_HOST} ^www\.(.+)$ [NC]
RewriteRule ^ https://%1%{REQUEST_URI} [L,R=301]

# ------------------------------------------------------------
# URLs limpas dos roteiros
# ------------------------------------------------------------
RewriteRule ^roteiros/?$              /roteiros.php [L]
RewriteRule ^roteiros/([a-z0-9-]+)/?$ /roteiro.php?slug=$1 [L,QSA]
RewriteRule ^sitemap\.xml$            /sitemap.php [L]

# ------------------------------------------------------------
# 301 do site WordPress antigo. Todas estas URLs eram 404 e
# jogavam fora anos de autoridade a cada recrawl.
# Destino proximo quando o roteiro existe; /roteiros quando morreu.
# ------------------------------------------------------------
RedirectMatch 301 ^/turquia/?$              /roteiros/turquia
RedirectMatch 301 ^/india-nepal/?$          /roteiros/caminhos-da-india
RedirectMatch 301 ^/japao-dubai-cingapura/?$ /roteiros/coreia-do-sul-japao-dubai

# a estatica aposentada e nova edicao da mesma viagem (confirmado com a cliente)
RedirectMatch 301 ^/tesouros-asiaticos\.html$ /roteiros/tesouros-asiaticos

# roteiros extintos e paginas de categoria -> a pagina mae
RedirectMatch 301 ^/(nossos-roteiros|europa|africa|asia-oriente-medio|america-do-sul|oceania|america-do-norte-caribe|outros-destinos)/?$ /roteiros
RedirectMatch 301 ^/(marrocos|capitais-imperiais|cruzeiro-pelo-danubio|viagem-transiberiana|terra_santa-jordania|rota-romantica-alemanha|expresso-luzes-do-norte)/?$ /roteiros
RedirectMatch 301 ^/(grecia-terra-mar|mercados-de-natal|floracao-das-cerejeiras|encantos-do-mediterraneo)\.html$ /roteiros

# o template dinamico antigo
RedirectMatch 301 ^/roteiro\.html$ /roteiros

# institucionais
RedirectMatch 301 ^/(nossa-historia|sobre-nos)/?$ /nossa-historia.html
RedirectMatch 301 ^/contato/?$                    /contato.html

# blog e versoes de idioma que nao existem mais
RedirectMatch 301 ^/(blog|dicas-de-viagem|6-dicas-para-uma-viagem-mais-proveitosa|meu-primeiro-post-do-blog)/?$ /
RedirectMatch 301 ^/(en|es)(/.*)?$ /
```

**Atenção à ordem:** `RewriteRule ^roteiros/...` vem **antes** do `RedirectMatch ^/(nossos-roteiros|...)`, e o `RedirectMatch ^/(nossa-historia|sobre-nos)/?$` casa **só com barra ou nada** — não pode capturar `/nossa-historia.html`, senão vira laço de redirect.

- [ ] **Step 2: Atualizar o robots.txt**

O `Disallow: /old/` pode sair (a pasta foi apagada pelo cliente em 16/07/2026). Manter `Disallow: /painel/` e a linha do `Sitemap:`.

- [ ] **Step 3: Backup do que está no ar**

```bash
SCRATCH="C:/Users/fabia/AppData/Local/Temp/claude/e--Clientes-Pereira-Oliveira-Turismo-po-tu-project/09986c54-cba0-4a97-ae6f-36dae87badaf/scratchpad"
cat > "$SCRATCH/.netrc" <<'EOF'
machine ftp.pereiraoliveiraturismo.com.br
login sitepo@pereiraoliveiraturismo.com.br
password <SENHA_FORNECIDA_NA_HORA>
EOF
chmod 600 "$SCRATCH/.netrc"
mkdir -p "$SCRATCH/backup"
curl -sS -k --ssl-reqd --netrc-file "$SCRATCH/.netrc" --ftp-pasv \
  "ftp://ftp.pereiraoliveiraturismo.com.br/.htaccess" -o "$SCRATCH/backup/.htaccess.bak"
ls -l "$SCRATCH/backup/.htaccess.bak"
```
Expected: arquivo não-vazio. **Sem este backup, não seguir.**

- [ ] **Step 4: Subir o PHP e os assets ANTES do .htaccess**

Ordem importa: com o `.htaccess` novo apontando para arquivos que ainda não subiram, o site serve 404. Subir primeiro:

```bash
FTP="ftp://ftp.pereiraoliveiraturismo.com.br"
N="$SCRATCH/.netrc"
up() { curl -sS -k --ssl-reqd --netrc-file "$N" --ftp-pasv --ftp-create-dirs -T "$1" "$FTP/$2" && echo "  subiu $2"; }

up lib/po-data.php            lib/po-data.php
up lib/po-view.php            lib/po-view.php
up roteiro.php                roteiro.php
up roteiros.php               roteiros.php
up sitemap.php                sitemap.php
up assets/css/roteiros.css    assets/css/roteiros.css
up assets/css/roteiro.css     assets/css/roteiro.css
up assets/js/roteiros-shared.js assets/js/roteiros-shared.js
up assets/js/roteiro.js       assets/js/roteiro.js
up assets/js/historia.js      assets/js/historia.js
up index.html                 index.html
up nossa-historia.html        nossa-historia.html
up contato.html               contato.html
up robots.txt                 robots.txt
up painel/app.js              painel/app.js
```

**NUNCA subir `config.local.php`.**

- [ ] **Step 5: Provar que o PHP responde antes de mexer no .htaccess**

```bash
curl -s -o /dev/null -w "roteiros.php -> %{http_code}\n" "https://pereiraoliveiraturismo.com.br/roteiros.php"
curl -s -o /dev/null -w "roteiro.php  -> %{http_code}\n" "https://pereiraoliveiraturismo.com.br/roteiro.php?slug=turquia"
curl -s "https://pereiraoliveiraturismo.com.br/roteiro.php?slug=turquia" | grep -o "<title>.*</title>"
```
Expected: `200`, `200`, e o title `Excursão Turquia 2026 · Viagem em grupo saindo de Florianópolis`. **Se qualquer um falhar, parar** — o `.htaccess` não vai consertar PHP quebrado.

- [ ] **Step 6: Subir o .htaccess e testar imediatamente**

```bash
up .htaccess .htaccess
bash tools/seo-check.sh https://pereiraoliveiraturismo.com.br
```
Expected: `0 falhas`.

**Se der 500 ou a suíte falhar, restaurar AGORA:**
```bash
curl -sS -k --ssl-reqd --netrc-file "$N" --ftp-pasv -T "$SCRATCH/backup/.htaccess.bak" "$FTP/.htaccess"
curl -s -o /dev/null -w "home apos rollback -> %{http_code}\n" "https://pereiraoliveiraturismo.com.br/"
```

- [ ] **Step 7: Verificar os 301, um a um**

```bash
for u in /turquia /india-nepal /japao-dubai-cingapura /nossos-roteiros /sobre-nos /contato \
         /europa /marrocos /blog /grecia-terra-mar.html /tesouros-asiaticos.html /roteiro.html; do
  printf '%-28s -> %s %s\n' "$u" \
    "$(curl -s -o /dev/null -w '%{http_code}' --max-redirs 0 "https://pereiraoliveiraturismo.com.br$u")" \
    "$(curl -s -o /dev/null -w '%{redirect_url}' --max-redirs 0 "https://pereiraoliveiraturismo.com.br$u")"
done
printf '%-28s -> %s %s\n' "www" \
  "$(curl -s -o /dev/null -w '%{http_code}' --max-redirs 0 'https://www.pereiraoliveiraturismo.com.br/')" \
  "$(curl -s -o /dev/null -w '%{redirect_url}' --max-redirs 0 'https://www.pereiraoliveiraturismo.com.br/')"
```
Expected: **todos `301`** com o destino da §4 da spec. `www` → `https://pereiraoliveiraturismo.com.br/`.

- [ ] **Step 8: Provar que não há laço de redirect**

```bash
for u in /nossa-historia.html /contato.html /roteiros /roteiros/turquia; do
  printf '%-24s -> %s (saltos: %s)\n' "$u" \
    "$(curl -s -o /dev/null -w '%{http_code}' -L "https://pereiraoliveiraturismo.com.br$u")" \
    "$(curl -s -o /dev/null -w '%{num_redirects}' -L "https://pereiraoliveiraturismo.com.br$u")"
done
```
Expected: todos `200` com **0 saltos**. Se `/nossa-historia.html` redirecionar, a regra do `sobre-nos` está capturando o `.html` — laço.

- [ ] **Step 9: Apagar o netrc**

```bash
rm -f "$SCRATCH/.netrc"
```

- [ ] **Step 10: Search Console**

- Reenviar `https://pereiraoliveiraturismo.com.br/sitemap.xml` (mesma URL de sempre, conteúdo novo).
- Inspecionar `/roteiros/turquia` e pedir indexação.
- Exportar **Páginas → Não indexadas** e conferir contra o mapa de `301`: qualquer URL antiga fora do mapa entra nele. O Wayback não era completo — o Search Console é a fonte definitiva.

- [ ] **Step 11: Atualizar o CLAUDE.md**

Na seção **Estado atual**, registrar:
- Roteiro agora é **server-side** (`roteiro.php` / `roteiros.php` / `sitemap.php`); o `roteiro-dynamic.js` e as 5 estáticas **não existem mais**. Nunca reintroduzir template em JS: fonte dupla foi o bug da home.
- **`ativo=eq.true` explícito é obrigatório** — em 16/07/2026 o banco tinha 0 linhas inativas, logo não há prova de que a RLS filtre rascunho.
- **Regra do slug:** identidade estável da viagem, uma edição ativa por slug, edição nova herda a URL. Palavra-chave em URL não rankeia.
- `prefers-reduced-motion` no hero passou a ser resolvido no **CSS** (`.hero__vid{display:none}`): o servidor não sabe a preferência, então o vídeo baixa e não aparece.
- Avaliações do Google = **fase 2**, e **sem `AggregateRating`** (self-serving review é violação de diretriz). Nota real: **5,0**.
- **Google Business possivelmente não reivindicado** — verificar com a cliente.

- [ ] **Step 12: Commit**

```bash
git add .htaccess robots.txt CLAUDE.md
git commit -m "feat: URLs limpas dos roteiros e 301 do site antigo

~20 URLs do WordPress eram 404 sem redirect, incluindo a /turquia
que rankeia hoje. 404 recrawleado perde autoridade de forma
irreversivel."
```

---

## Self-Review

**Cobertura da spec:**

| Requisito da spec | Task |
|---|---|
| §3 `roteiro.php` server-side, cache, 404 real | 2, 4 |
| §3 `/roteiros` página mãe | 5 |
| §3 `sitemap.php` do banco | 6 |
| §3 limpeza (5 estáticas, catálogos, `poRoteiroHref`) | 7 |
| §4 mapa de `301` + www | 10 |
| §5 `<title>` com "excursão"/ano/Florianópolis; `<h1>` da mãe | 3, 5 |
| §6 renomeação de slug + índice parcial + truncagem | 8, 9 |
| §7 Google Business | fora do código — Task 10 Step 11 registra |
| §8 fase 2 (avaliações) | fora de escopo |
| §9 critérios 1-7 | `tools/seo-check.sh` (Task 1) + Task 10 Steps 5-8 |
| §9 critério 8 (Search Console) | Task 10 Step 10 |

**Consistência de tipos:** `po_e`, `po_url`, `po_roteiro_href`, `po_fetch_roteiro(s)`, `po_ano`, `po_titulo_seo`, `po_head`, `po_header`, `po_footer`, `po_wa_float`, `po_arr` — usados com a mesma assinatura em todas as tasks. `PO_TEST` guarda o despacho nos três arquivos web.

**Riscos aceitos e registrados:**
- `prefers-reduced-motion` no hero vira CSS (baixa o vídeo, não mostra). Task 4 Step 3.
- Markup portado do JS para PHP: o CSS não muda, mas divergência de classe quebra o layout em silêncio. Mitigado pela conferência visual (Task 4 Step 5 e Task 5 Step 5).
- `.htaccess` pode dar 500. Mitigado por backup + suíte imediata + rollback (Task 10 Steps 3, 6).
- O inventário de URLs antigas veio do Wayback e **não é completo**. Task 10 Step 10 fecha isso com o Search Console.
